const LoadingSpinner = () => (
  <div
    role="status"
    data-testid="loading-spinner"
    className="animate-spin h-5 w-5 border-2 border-slate-900 border-t-transparent rounded-full mx-auto"
    aria-label="Loading"></div>
);

export default LoadingSpinner;
